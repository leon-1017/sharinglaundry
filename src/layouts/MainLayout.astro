---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import ScrollReveal from "../components/ScrollReveal.astro";
import BackToTop from "../components/BackToTop.astro";
import "../styles/global.css";

const {
  title = "Sharing Laundry",
  description = "Commercial and industrial laundry equipment static site",
  image = "",
  canonical = "",
  schemas = [],
} = Astro.props;

const canonicalUrl = canonical
  ? new URL(canonical, Astro.site ?? Astro.url).toString()
  : Astro.url.href;

const ogImageUrl = image
  ? new URL(image, Astro.site).toString()
  : new URL("/wp-assets/uploads/SWE25S.png", Astro.site).toString();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url.href} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:site_name" content="Wuxi Sharing Machinery Co.,Ltd" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    {schemas.map((schema) => (
      <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ))}
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonicalUrl} />
  </head>
  <body>
    <div class="scroll-strip" aria-hidden="true"></div>
    <Header />
    <slot />
    <Footer />
    <ScrollReveal />
    <BackToTop />
    <script is:inline>
      (function () {
        function updateHeader() {
          const header = document.querySelector('header');
          if (!header) return;
          const y = window.scrollY;
          const scrolled = header.classList.contains('is-scrolled');
          if (!scrolled && y > 80) {
            header.classList.add('is-scrolled');
          } else if (scrolled && y < 40) {
            header.classList.remove('is-scrolled');
          }
        }
        function updateScrollStrip() {
          const strip = document.querySelector('.scroll-strip');
          if (!strip) return;
          const max = document.documentElement.scrollHeight - window.innerHeight;
          const p = max > 0 ? Math.min(window.scrollY / max, 1) : 0;
          strip.style.setProperty('--scroll-progress', String(p));
        }
        updateHeader();
        updateScrollStrip();
        window.addEventListener('scroll', () => {
          updateHeader();
          updateScrollStrip();
        }, { passive: true });
      })();
    </script>
  </body>
</html>
